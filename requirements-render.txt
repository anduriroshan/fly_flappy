# Offline morphology-render extras. Install on the machine that fetches
# skeletons from neuprint (needs NEUPRINT_TOKEN). Blender is a SEPARATE
# install (not pip) — get it from blender.org or your package manager.
#
#   pip install -r requirements-render.txt
#
navis>=1.5
neuprint-python>=0.4.26
# navis pulls these, but pin them so skeleton healing / SWC IO is available:
networkx>=3.1
trimesh>=4.0
