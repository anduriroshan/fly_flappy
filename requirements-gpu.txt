# GPU-server (CUDA 12.1) requirements.
# Install: python -m pip install -r requirements-gpu.txt
pytest>=7.0    # `python -m pytest tests` is the verification gate in RUNBOOK.md
numpy>=1.24,<2.0
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65
requests>=2.28    # gcs_fetch.py's public-bucket skeleton fetch (default morphology path)
opencv-python-headless>=4.8    # headless OpenCV for servers without GUI
gymnasium>=0.29
flappy-bird-gymnasium>=0.4.0
stable-baselines3>=2.3
sb3-contrib>=2.3
networkx>=3.1
matplotlib>=3.7
tensorboard>=2.15
--extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.2
# PyTorch Geometric is optional; used only if you swap the sparse
# MM connectome for a GNN-style message-passing update.
torch-geometric>=2.5
